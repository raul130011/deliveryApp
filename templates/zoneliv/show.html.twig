{% extends 'base.html.twig' %}

{% block title %}Zoneliv{% endblock %}

{% block body %}
    <h1>Zoneliv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zoneliv.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ zoneliv.name }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ zoneliv.kilometrage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zoneliv_index') }}">back to list</a>

    <a href="{{ path('app_zoneliv_edit', {'id': zoneliv.id}) }}">edit</a>

    {{ include('zoneliv/_delete_form.html.twig') }}
{% endblock %}
